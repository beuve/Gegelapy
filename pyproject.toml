[build-system]
requires = ["scikit-build-core>=0.5.0", "pybind11", "ninja", "cmake"]
build-backend = "scikit_build_core.build"

[project]
name = "gegelapy"
version = "0.1.0"

[tool.scikit-build]
cmake.args = ["-G Ninja"]
wheel.expand-macos-universal-tags = true
